<div class="container">
  <h1>Lists</h1>


    <% @lists.each do |list| %>
    <div class="card m-2 btn w-50">
    <%= link_to list.name, list_path(list)  %>
      </div>
    <% end %>

    <%# <%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>

</div>
